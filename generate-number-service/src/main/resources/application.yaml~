spring:
  application:
    name: generate-number-service
  liquibase:
    enabled: true
  datasource:
    url: jdbc:postgresql://generate-db:5432/generate-number-service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  docker:
    compose:
      enabled: true
server:
  port: 8080